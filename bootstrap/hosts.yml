# All hosts that are to be provisioned
all:
  hosts:
    # 192.168.122.32:
  children:
    provision:
      hosts:
        192.168.122.195:
          hostname: archtest_fde
          partition: true
          timezone: "US/Eastern"
          partition_options:
            disk: /dev/vda
            # swap: true
            # swap_size: 8
            # separate_home: true
            # root_size: 32
            # encrypt: true
          disksetup_flow: cryptlvm
          bootstrap_packages:
            - intel-ucode
    postinstall:
      hosts:
        192.168.122.32