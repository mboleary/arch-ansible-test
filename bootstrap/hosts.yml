# All hosts that are to be provisioned
all:
  hosts:
    # 192.168.122.32:
  children:
    provision:
      hosts:
        172.31.1.31:
          hostname: arch-provision-host
          partition: true
          timezone: "US/Eastern"
          partition_options:
            disk: /dev/sda
            # swap: true
            # swap_size: 8
            # separate_home: true
            # root_size: 32
            # encrypt: true
          disksetup_flow: crypt_lvm
          volgroup_name: provision_volgrp
          bootstrap_packages:
            - amd-ucode
    postinstall:
      hosts:
        # 172.31.1.233