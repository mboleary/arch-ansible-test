# All hosts that are to be provisioned
all:
  hosts:
    # 192.168.122.32:
  children:
    provision:
      hosts:
        # 172.31.1.174:
        #   hostname: brady-fw13
        #   partition: true
        #   timezone: "US/Eastern"
        #   partition_options:
        #     disk: /dev/nvme0n1
        #     # swap: true
        #     # swap_size: 8
        #     # separate_home: true
        #     # root_size: 32
        #     # encrypt: true
        #   swap_size: 32G
        #   disksetup_flow: crypt_lvm
        #   volgroup_name: provision_volgrp
        #   bootstrap_packages:
        #     - intel-ucode
        #     - amd-ucode # Will eventually be transferred over to amd motherboard
    postinstall:
      hosts:
        172.31.1.175