# All hosts that are to be provisioned
all:
  hosts:
    # 192.168.122.32:
  children:
    provision:
      hosts:
        # 172.31.1.233:
        #   hostname: archtest_wifi
        #   partition: true
        #   timezone: "US/Eastern"
        #   partition_options:
        #     disk: /dev/sda
        #     # swap: true
        #     # swap_size: 8
        #     # separate_home: true
        #     # root_size: 32
        #     # encrypt: true
        #   disksetup_flow: singlepart
        #   bootstrap_packages:
        #     - intel-ucode
    postinstall:
      hosts:
        # 172.31.1.203